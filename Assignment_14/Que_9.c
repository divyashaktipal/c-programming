// Date-..../10/2022
// Created by- Divyashakti Pal

/*  QUESTION.

Write a function to check whether a given number contains a
given digit or not. Write a function and pass the ‘number’ and the
‘digit’ as function arguments. In the function definition, receive
the value of arguments in formal parameters. Now in the function,
check whether a given number contains a digit or not. Return the
result. For example, the function will return ‘1’ if the digit is
present, otherwise it will return 0. Now in main() function receive
this result and print message accordingly. If function return ‘1’
then it should print the message DIGIT IS PRESENT IN THE
NUMBER. Otherwise it should print DIGIT IS NOT PRESENT IN THE
NUMBER.

*/
#include <stdio.h>
int main()
{

    return 0;
}