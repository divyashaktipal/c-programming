#include <stdio.h>
int main()
{
    // NOW HOW TO PRINT FOUR VARIABLE
    int hi, how, are, you;
    hi = 15, how = 16, are = 17, you = 18;
    printf("first variable holds an integer constant is %d\n", hi);
    printf("second variable holds an integer constant is %d\n", how);
    printf("third variable holds an integer constant is %d\n", are);
    printf("fourth variable holds an integer constant is %d\n\n", you);

    printf("WELCOME");
    return 0;
}