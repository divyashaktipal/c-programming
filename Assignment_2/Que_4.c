#include <stdio.h>
int main()
{
    // NOW HOW TO PRINT THIPLE VARIABLE
    int raju, ramesh, akash;
    raju = 5, ramesh = 6, akash = 7;
    printf("value of the first variable is %d & value of the second variable is %d and value of the third variable is %d\n\n", raju, ramesh, akash);

    return 0;
}