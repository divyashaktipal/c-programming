// Date-22/10-2022
// created by- DIVYASHAKTI PAL

#include <stdio.h>
int main()
{
    /*  QUESTION.
    Write a menu driven program with the following options:
    1. Addition
    2. Subtraction
    3. Multiplication
    4. Division
    5. Exit
    */

    int n, l, a, b, m;
    switch (n)
        printf("Enter 1 to 5 number\n");
    {
    case 1:
        printf("Enter any integer \n");
        scanf("%d %d", &l, &a);
        printf("addition of these number is %d", l + a);
        break;
    case 2:
        printf("Substraction of these number");
        scanf("%d %d", &b, &m);
        printf("substraction of these number is %d", b * m);
        break;

        return 0;
    }